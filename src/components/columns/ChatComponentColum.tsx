import {StyleSheet, Text, View} from 'react-native';
import React, {FC} from 'react';

const ChatComponentColum: FC = () => {
  return (
    <View>
      <Text>ChatComponentColum</Text>
    </View>
  );
};

export default ChatComponentColum;

const styles = StyleSheet.create({});
